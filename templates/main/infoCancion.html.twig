{% extends 'main/main.html.twig' %}
{% block body %}
    <h3>{{cancion.name}}</h3>
    
   
        
         <p>Album:{{cancion.album}}</p>
        <p>Banda:{{cancion.band}}</p>
      
        <p>Año:{{cancion.year}}</p>
        <img src="{{cancion.img}}" />
        

    
{% endblock %}